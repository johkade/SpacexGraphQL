import * as React from 'react';
import {View} from 'react-native';
import 'react-native-tailwind.macro';

export type <%= props.pascalCaseName %>Props = {
  //
};

const <%= props.pascalCaseName %> = ({}: <%= props.pascalCaseName %>Props) => {
  return (
    <View tw="">
      <View />
    </View>
  );
};

export default <%= props.pascalCaseName %>;
